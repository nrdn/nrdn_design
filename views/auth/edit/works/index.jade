extends ../../../layout

block content_style
	link(rel='stylesheet', type='text/css', href='/styles/add.css')

block content_scripts
	script(type='text/javascript', src='/scripts/main.js')
	//- script(type='text/javascript', src='/scripts/masonry.pkgd.min.js')

block content_right

block content
	a.back_link(href='/auth') < Назад
	.title СПИСОК РАБОТ
	br
	each work in works
		a(href='#')= 'удалить '
		||&nbsp;
		a(href='/auth/edit/works/' + work._id)= work.ru.title