extends ../layout

block content_style
	link(rel='stylesheet', type='text/css', href='/styles/add.css')

block content
	a.back_link(href='/logout') Выход
	br
	//- b= session
	//- br
	.title ЗВЕЗДОЧЕТ
	br
	b Работы:
	br
	a(href='/auth/add/work')= ' добавить '
	||
	a(href='/auth/edit/works')= ' редактировать'
	br