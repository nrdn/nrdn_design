$(document).ready(function() {
  var s = skrollr.init({
  	forceHeight: true
  });

  // $('.work').on({
  // 	mouseover: function() {
  // 		$(this).children('.w_description').show();
  // 	},
  // 	mouseout: function() {
  // 		$(this).children('.w_description').hide();
  // 	}
  // });
});